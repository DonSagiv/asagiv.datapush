<Window x:Class="asagiv.datapush.ui.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:asagiv.datapush.ui.ViewModels"
		xmlns:views="clr-namespace:asagiv.datapush.ui.Views"
		mc:Ignorable="d"
        d:DataContext="{d:DesignInstance vm:MainWindowViewModel, IsDesignTimeCreatable=False}"
		WindowStartupLocation="CenterScreen"
        Title="MainWindow"
		Height="600"
		Width="800">

	<TabControl DockPanel.Dock="Top"
				Margin="10">

		<TabItem Header="Windows Client"
				 x:Name="WindowsClientTab">

			<views:WindowsClientView x:Name="WindowsClientView"/>

		</TabItem>

		<TabItem Header="Settings"
				 x:Name="SettingsTab">

			<DockPanel>

				<GroupBox Margin="10"
						  DockPanel.Dock="Top"
						  Header="Windows Service Settings">

					<views:WindowsServiceView x:Name="WindowsServiceView"/>

				</GroupBox>

				<GroupBox Margin="10"
						  Header="Connection Settings">

					<views:ConnectionSettingsView x:Name="ConnectionSettingsView"/>

				</GroupBox>

			</DockPanel>

		</TabItem>

	</TabControl>

</Window>
